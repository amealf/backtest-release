# V4.3 terminal management snapshot

This directory preserves the active bilingual V4.3 management records exactly as copied before V4.4 became current. Filenames encode their former relative directories with `__`. These files are historical evidence and are not active V4.4 instructions.

