# 历史决策

## 决策登记

| 日期 | 状态 | 决策 | 理由 | 复查条件 |
| --- | --- | --- | --- | --- |
| 2026-08-02 | 当前 | max-W campaign 在终局 Round 5 后闭合。保留三个不同的视图专属样本内候选：情景一总收益使用 Round 4 的 E30/BH720/TRW6/K1.5/W1/M2；无限制总收益使用 Round 5 的 E120/BH360/TRW6/K0.75/W1/M0.25；两个笔均收益视图使用 Round 3 的 E200/BH720/TRW24/K0.75/W192/M10。不建立 Round 6，不接受参数。 | Round 5 已闭合全部批准区块与唯一一次交付。情景一与两个笔均收益视图没有替换既有领导者；无限制总收益改善到 515.916078%。继续扩大样本内搜索会超出批准的终局协议。 | 用户另行授权具有独立身份与接受标准的外部验证或新研究契约。 |
| 2026-08-02 | 已履行 | 运行已批准的终局 24 坐标 Round 5，只为情景一总收益与无限制总收益建立两个彼此独立的 12 点块。保持 all-window／rolling-only／S480、四独立视图、仅显示用 gap 审计、3／12／4096、精确反连接、无笔均收益块、无 Round 6 与无参数接受。 | Round 4 已稳定闭合；只有情景一与无限制总收益通过各自精确续轮规则。两个笔均收益视图都低于累计 M10 领导值并停止。 | 保留为已完成的 Round 5 操作决定。 |
| 2026-08-02 | 当前 | 运行一次用户授权、有边界的 72 坐标 Round 4，包含三个彼此独立的终局领导者边界块。保留四视图、all-window／rolling-only／S480、仅显示用 gap 审计、3／12／4096 资源、精确反连接、compute-only 交付抑制与无参数接受。 | 终局领导者触及尚未解决的 E/W/K、E/W/M 与 M 边界；用户审阅证据后明确替换原有停止决定。 | Round 4 证据闭合后触发已记录的停止决定，或另行批准条件 Round 5。 |
| 2026-08-02 | 已反转 | max-W campaign 在终止轮 Round 3 闭合，不建立 Round 4。 | 该决定在旧契约下正确；用户后来明确授权一次有界续轮。 | 只保留为历史证据边界；它不再阻止已批准的 Round 4。 |
| 2026-08-02 | 当前 | max-W campaign 使用四个独立当前排名视图：情景一合格坐标的总收益，不增加笔数下限；无限制总收益，不使用情景或笔数下限；`>=10` 的无限制笔均收益；`>=20` 的无限制笔均收益。每个视图按主指标与 `combo_id` 排序；诊断指标保留为表格。 | 精确保留业务问题，避免综合分数或由诊断指标改变排名。 | 用户改变排名契约。 |
| 2026-08-02 | 当前 | Gap-excluded return 只作明确标注、显示用的 gap 依赖审计。它不能过滤或排名情景一、任何主视图、续轮、seed 选择或候选考虑。 | 已批准的排名契约使用完整 `train_return` 与 `train_avg_trade`；gap 依赖保留诊断价值，同时不改变目标。 | 用户改变 gap 审计契约。 |
| 2026-08-02 | 当前 | 将 Round 1 归类为 12 坐标实现／smoke probe；Round 2 使用确定性的 504 坐标 `all_window` E/BH/TRW/K/W/M 笛卡尔探索并固定 S480；Round 3 在 48–96 坐标范围内围绕四个视图分别记录的领导者进行有意义的交互细化。样本内证据不能自动接受参数。 | 明确的多轮目标要求宽广、可复现的覆盖，随后提供足够的局部 E/W/threshold 或 M 交互覆盖，避免象征性的单轴 probe。 | Round 3 设计获批，或用户改变 campaign 范围。 |
| 2026-08-02 | 当前 | 使用基于指定市场选定器建立的版本中立场景管理器；保存时采用自动连续的 `情景N` 名称，并从两行场景按钮中选取已保存场景。 | 场景定义需要跨 V4.2、V4.3 与后续版本保持可编辑、可发现，同时不改写历史结果快照，也不要求手动命名。 | 用户改变共享场景流程或存储边界。 |
| 2026-08-02 | 当前 | 保持 `rolling_tr_sum` 为唯一聚合方法；将 `all_window`（默认）与 `exclude_marked`（可选）作为独立 baseline 采样策略，使用不同身份与排名。 | 两种采样选择都可能使用，同时需要保持证据可比，且不能恢复已退役的 TR 平均公式。 | 用户改变 baseline 采样或批准第三种策略。 |
| 2026-08-02 | 当前 | 数据准备层发布策略中性标记；计划／坐标决定标记如何影响 baseline 合格判断。 | 同一份准备产物需要支持当前两种策略，不能为每个选择重复生成。 | 标记 schema 或受支持策略集合变化。 |
| 2026-08-02 | 当前 | 当前 synthetic 零 TR 纳入行为保持不变；real-only 或排除 synthetic baseline 只属于诊断范围。 | 量化影响显著，但两种可能的 real-only 采样定义属于不同研究契约。 | 用户批准一种明确 synthetic 处理方式。 |
| 2026-08-02 | 当前 | 来源 datetime 重复时停止；不允许隐含 keep-first 或 keep-last。 | 静默保留／合并会破坏连续性与来源对齐。 | 来源具备明确 contract／instrument key 与已审阅去重政策。 |
| 2026-08-01 | 当前 | 在新文件夹建立 V4.3，保留 V4.2，并使用新的策略／结果／组合／审计身份。 | 平仓执行语义变化后，证据不能混用。 | 用户启动后续版本。 |
| 2026-08-01 | 当前 | pending entry 按第一根真实成交 bar open 成交，不增加价格重触发、创新高取消或结构反转取消。 | 这是用户确认的开仓解释。 | 用户明确改变开仓执行。 |
| 2026-08-01 | 当前 | rebound 或 speed 信号在无真实成交的 bar 上转为 pending，并按下一根真实成交 bar open 成交。 | 无真实成交的 bar 不能提供可执行成交。 | 用户明确改变平仓执行。 |
| 2026-08-01 | 当前 | 活跃运行输入使用仓库相对路径；旧绝对路径只作来源记录。 | V4.3 文件夹必须可移动、可复现。 | 运行打包要求变化。 |
| 2026-08-01 | 当前 | 替换指定项目或扩大需求前取得确认。 | 用户指定的现有工具／模板／行为定义预期结果。 | 用户改变此沟通规则。 |
| 2026-08-01 | 当前 | 用户要求 ZIP 时，包含当前版本代码、项目管理文件、分析报告与 15 秒 OHLC 数据，同时排除运算结果载荷。 | 保留可复现交接内容，同时不复制 campaign 输出。 | 用户改变打包契约。 |
| 2026-08-01 | 当前 | 英文管理文档是执行来源，中文文件是审阅镜像。 | Agent 获得单一执行来源，同时保留双语人工审阅。 | 项目负责人改变语言规则。 |
| 2026-08-01 | 当前 | 根目录 Agent 规则保持简明，详细上下文保存在 `project_management`。 | 高优先级指令保持易读，细节仍可发现。 | 工作区指令机制变化。 |
| 2026-08-01 | 当前 | 已有文件内容不同时生成可审阅的提案文件。 | 初始化期间不能替换现有项目内容。 | 用户批准特定合并规则变化。 |
| 2026-08-01 | 当前 | 只有已确认项目领域才添加领域文档。 | 空类别会误导后续工作并浪费上下文。 | 项目采用新领域。 |

## 新增决策

只记录影响后续工作的长期选择。包含决策内容、证据或理由、状态、日期与复查条件。临时参数与任务状态写入 `03_active_work\CURRENT_TASKS.en.md`。

## 状态词汇

使用「提案」「当前」「已退役」或「已反转」。提案不能作为当前工作规则。
