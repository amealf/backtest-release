# Retained V4.4 cost-adjusted multi-round reports

These nine Markdown reports were copied byte-for-byte on 2026-08-08 from the retained Agent artifact directory into the project-owned handoff tree before local Agent state was recycled.

They remain historical analysis evidence for the V4.4 strategy-major lineage and are included in V4.41 handoff packages. The package builder verifies every report copy by SHA-256. This directory replaces `.omo` as the stable package source.
